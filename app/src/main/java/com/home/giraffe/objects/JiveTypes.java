package com.home.giraffe.objects;

public enum JiveTypes {
    Unknown,
    JiveMessage,
    JiveInstance,
    JiveGroup,
    JivePerson,
    JiveDiscussion,
    JiveFile,
    JiveSpace,
    JiveProject,
}
