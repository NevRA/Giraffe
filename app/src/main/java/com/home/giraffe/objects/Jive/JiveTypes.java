package com.home.giraffe.objects.Jive;

public enum JiveTypes {
    Unknown,
    JiveMessage,
    JiveComment,
    JiveInstance,
    JiveGroup,
    JivePerson,
    JiveDiscussion,
    JiveDocument,
    JiveFile,
    JiveSpace,
    JiveProject,
    JivePoll,
    JiveTask,
    JiveLevel,
    JivePost,
}
