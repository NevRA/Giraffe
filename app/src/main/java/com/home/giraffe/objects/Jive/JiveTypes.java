package com.home.giraffe.objects.Jive;

public enum JiveTypes {
    Unknown,
    JiveMessage,
    JiveComment,
    JiveInstance,
    JiveGroup,
    JivePerson,
    JiveDiscussion,
    JiveFile,
    JiveSpace,
    JiveProject,
    JiveLevel,
}
